{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "rust: cargo build --bin view",
      "type": "shell",
      "command": "cargo build --bin view"
    },
    {
      "label": "rust: cargo build --bin view-ash",
      "type": "shell",
      "command": "cargo build --bin view-ash"
    }
  ]
}
